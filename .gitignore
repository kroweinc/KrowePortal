.claude/settings.local.json

# Environment — never commit secrets. Keep an .env.example for documentation.
.env
.env.*
!.env.example

node_modules/
.next/

# TypeScript incremental build cache
*.tsbuildinfo

# Next.js auto-generated; regenerated on every build
next-env.d.ts

# Local screenshot capture output (regenerate via scripts/capture-screens.mjs)
capture/
